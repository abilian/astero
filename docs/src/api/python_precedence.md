# `astero.python.precedence`

Declares Python's operator precedence and the three questions that read it. The
bracketing *rule* is language-agnostic and lives in
[`emit`](emit.md); what binds tighter than what is a fact about Python.

::: astero.python.precedence
