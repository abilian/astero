# `astero.python`

Declares Python's grammar, its scope tables, and its namespaces. Python is the one language that ships with the library; everything else in `astero` is language-agnostic.

::: astero.python.lang
