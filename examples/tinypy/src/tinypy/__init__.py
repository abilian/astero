"""A compiler for a Python subset, with two back ends, built on astero.

A package for the same reason `pl0` is: top-level modules on `sys.path`
shadow standard ones, and `compiler` is a name worth not gambling on.
"""

from __future__ import annotations
